# Publishing credentials. Copy to .env, which is gitignored.
#
# A granular access token from npmjs.com with write access to the @heojeongbo
# scope. It is the only secret this repository needs, and it lives here and
# nowhere else -- .npmrc references it as ${NPM_TOKEN}.
NPM_TOKEN=
